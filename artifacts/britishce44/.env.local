# ─── Local Mode ──────────────────────────────────────
# Only core features that work fully offline with PostgreSQL.
# Online features (AI, Google, Video) gracefully degrade.

VITE_ONLINE_MODE=false
BASE_PATH=/
PORT=8080
# Optional: TURN server for WebRTC behind NAT (required for P2P classroom video)
# VITE_TURN_SERVERS=[{"urls":"turn:localhost:3478","username":"britishce44","credential":"turn_secret_2026"}]
